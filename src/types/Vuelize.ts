export enum State {
    success = "success",
    error = "error",
    warning = "warning",
    info = "info"
}

export enum Rounded {
    circle = "circle",
    pill = "pill",
    xl = "xl",
    lg = "lg",
    md = "md",
    none = "none"
}

export enum PositionVertical {
    top = 'top',
    bottom = 'bottom',
}

export enum PositionHorizontal {
    left = 'left',
    center = 'center',
    right = 'right'
}
export enum Position {
    top = 'top',
    bottom = 'bottom',
    left = 'left',
    right = 'right',
}
